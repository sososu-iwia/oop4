-- PostgreSQL больше не используется.
-- Приложение использует встроенную БД H2 в памяти (см. application.properties).
-- Таблицы создаются автоматически при запуске (JPA + schema.sql).
-- Никаких действий с этой папкой не требуется.
